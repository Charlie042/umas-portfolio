import { use } from "react";
import { fetchFeaturedWorkData } from "@/components/apiAction";

export function FeaturedWorkAdd(){
    // const featuredCards = use(fetchFeaturedWorkData());

    return (
        <div>
            <h1>Featured Work Add</h1>
        </div>
    )
}